[package]
name = "gguy-godot"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
godot = { version = "=0.5.3", features = ["api-4-4"] }
gguy-core = { path = "../gguy-core" }
